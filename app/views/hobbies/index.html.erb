<div class="container centered-content text-center-content">
  <h1 class="text-2xl font-bold mb-4"><%= current_user.name %>さん's♡</h1>
    <div class="flex flex-wrap gap-4 justify-center">
      <% @hobbies.each do |hobby| %>
        <%= render partial: 'shared/hobby', locals: { hobby: hobby, user: @user } %>
      <% end %>
    </div>
  <%= link_to '♡追加', new_user_hobby_path(current_user), class: 'btn btn-primary' %>
</div>